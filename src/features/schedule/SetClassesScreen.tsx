/** Phase 06: master-schedule-powered entry; resolves each class by teacher/room; saved on-device. */
export function SetClassesScreen() {
  return (
    <section className="screen" aria-labelledby="set-classes-title">
      <h1 id="set-classes-title" className="screen__title">
        Set Classes
      </h1>
      <p className="screen__sub">Enter your classes so the map can show you where to go.</p>
    </section>
  );
}
