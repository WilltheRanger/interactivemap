/**
 * Map screen (shell stub).
 * Phase 05 renders the campus SVG in Leaflet with clickable named building shapes (id = buildings.id),
 * a multi-level toggle, and the reusable highlight API.
 */
export function MapScreen() {
  return (
    <section className="app-screen" aria-labelledby="map-title">
      <h1 id="map-title">Map</h1>
      <p>Interactive campus map. (Coming in Phase 05.)</p>
    </section>
  );
}
